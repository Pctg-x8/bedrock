[package]
name = "wayland_examples"
version = "0.1.0"
edition = "2021"

[dependencies]
bedrock = { path = "..", features = [
    "Implements",
    "Presentation",
    "VK_KHR_wayland_surface",
    "VK_EXT_debug_utils",
    "Allow1_3APIs",
] }
